// ContextPanel is replaced by BlossomForm + SceneOverlay in the new world-as-interface design.
// Kept as an empty stub so nothing breaks during a partial refactor.
export default function ContextPanel() { return null }
